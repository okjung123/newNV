<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

	<!-- 여러가지 기본 옵션을 지정합니다. -->

	<settings>
		<!-- 필드 속성이 READ_COUNT 처럼 언더 스코어가 있을 때 VO 의 readCount 처럼 카멜 케이스로 변환 되게 합니다. -->
		<setting name="mapUnderscoreToCamelCase" value="true"/>
	</settings>

	<!-- 매퍼에서 사용할 vo 클래스에 대한 줄임말 선언 -->
	<typeAliases>
		<typeAlias alias="UserVO" type="rm.web.main.vo.UserVO"/>
	</typeAliases>

	<!-- mapper : 실행할 쿼리문만 별도로 작성하는 파일을 말함 -->
		<mappers>
  			<mapper resource="mappers/MainMapper.xml" /> 
		</mappers>
	
</configuration>